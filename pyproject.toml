[tool.poetry]
name = "parallel-parrot"
version = "0.0.1"
description = ""
authors = ["Brad Ito <brad@novex.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
